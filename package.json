{
  "name": "stamp-action",
  "version": "1.0.0",
  "description": "Build5 stamp action",
  "main": "lib/index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "build": "tsc",
    "build:prod": "tsc && ncc build lib/index.js --license licenses.txt && rm -rf lib",
    "clean": "rm -rf node_modules; rm -rf lib; rm -rf dist",
    "start": "tsc && node lib/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  }
}
